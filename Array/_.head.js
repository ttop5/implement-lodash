/***
* 获取数组 array 的第一个元素。
*
* @param  {Array} array [description]
* @return {Number}      [description]
* @example
*
* head([1, 2, 3]);
* // => 1
*
* head([]);
* // => undefined
*/
function head(array) {
  return array[0];
}
