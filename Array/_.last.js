/**
* 获取array中的最后一个元素。
*
* @param  {Array}  array 要检索的数组
* @return {Number}       返回array中的最后一个元素
* @example
*
* last([1, 2, 3]);
* // => 3
*/
function last(array) {
  return array[array.length-1];
}
