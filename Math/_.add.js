/**
* 两个数相加。
*
* @param  {Number} augend 被加数
* @param  {Number} addend 加数
* @return {Number}        返回总和
* @example
*
* add(6, 4);
* // => 10
*/
function add(augend, addend) {
  return (augend + addend);
}
