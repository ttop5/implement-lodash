/**
 * 从string字符串中移除前面和后面的空格。
 *
 * @param   {string} [string=''] 要处理的字符串
 * @returns {string}             返回处理后的字符串
 * @example
 *
 * trim('  abc  ');
 * // => 'abc'
 */

 function trim(string) {
   return string.trim();
 }
